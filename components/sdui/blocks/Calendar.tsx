import type { CalendarProps } from "@/types/blocks";

export default function Calendar(_props: CalendarProps) {
  return (
    <div className="p-4 my-2 border-2 border-dashed border-gray-400 rounded">
      <code className="text-sm text-gray-500">[stub] blocks.calendar</code>
    </div>
  );
}
